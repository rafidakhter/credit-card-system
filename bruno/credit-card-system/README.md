# Bruno API Tests

Use this Bruno collection to test the credit card system API.

## Local Setup

Start the API:

```bash
cd apps/api
npm run start:dev
```

Open this folder in Bruno:

```text
bruno/credit-card-system
```

Select the `local` environment.

## Current Requests

- `Health Check`: `GET {{baseUrl}}/health`

